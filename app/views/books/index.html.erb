<div>

  <div>
    <a href='/books/new'>Submit a new book</a>
  </div>

  <% @books.each do |book| %>
    <h2>
      <a href="/books/show?id=<%= book.id %>"> <%= book.title %> </a>
    </h2>
  <% end %>
</div>
